# Copy this file to .env and edit the values there. .env is not committed.

# Where the Ollama server is. Leave as localhost when using an SSH tunnel
# to the lab machine (see README).
OLLAMA_HOST=http://localhost:11434

# Where the RIMAPI mod's REST server is (RimWorld running on this machine).
RIMAPI_URL=http://localhost:8765
